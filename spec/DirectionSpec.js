describe('Direction', function () {
    it('has a static property LEFT', function () {
        expect(Direction.LEFT).toBe(1);
    });

    it('has a static property UP', function () {
        expect(Direction.UP).toBe(2);
    });

    it('has a static property RIGHT', function () {
        expect(Direction.RIGHT).toBe(3);
    });

    it('has a static property DOWN', function () {
        expect(Direction.DOWN).toBe(4);
    });
});
